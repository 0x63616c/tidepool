# Tidepool — Session Handoff (2026-06-28)

Continue from here. The mission is **Phase C**: prove a coding agent (opencode) runs on a **real Hetzner worker box**, produces a merged PR on `0x63616c/tidepool-testbed`, the box is destroyed, and `tp doctor` exits 0. Reference docs (don't re-read unless needed): `GOALS.md`, `DESIGN.md`, `HANDOFF.md`, `AGENTS.md` (tenets/standards), `RESEARCH.md`, `SKILLS.md` in repo root `/Users/calum/code/github.com/0x63616c/tidepool`.

## Operating model (how Calum wants this run)
- You are the **lead/manager**: delegate to sub-agents (Agent tool), validate their evidence yourself, keep your own context lean. Calum got annoyed when the lead grinded inline at the keyboard.
- A delegating sub-agent named **`conductor`** (orange) has been driving execution and spawning its own workers. It currently holds the failing `tp run` output in its context and is idle — you can resume it for the error, or re-instrument yourself.
- **Verify, don't trust** sub-agent "green" claims (a prior lead relayed a "live box verified" claim that ground-truth didn't support). Run the gates yourself.

## Current TRUE state (ground-truthed, not claimed)
- **Phase A** ✅ merged (PR #1 core, PR #2 coverage — 48 behaviour tests).
- **Phase B** ✅ merged (PR #3 real adapters). `slugify` IS on testbed `main` (testbed PR #1, ticket `tckt_96ot0e5rzm`) — but produced by the **LocalBoxMaker (laptop)** run, NOT Hetzner.
- **Phase C** 🔴 **NOT proven.** HetznerBoxMaker built + merged (PR #4, #5). The real Hetzner e2e was run **twice** for the "add truncate" ticket (`tckt_qt6tbzn900`) and **FAILED both times**:
  - Box DID provision and run (e.g. `tp-worker-nxtwgic4wz` @ ephemeral IP, labels `managed_by=tidepool role=worker`).
  - But **no branch was ever pushed to testbed**, **no work-run row recorded**, ticket ended `failed` (attempts=2). Box was cleaned up each time — **0 Hetzner servers now, no leak** (teardown/acquireRelease works).
- **Open: PR #7** https://github.com/0x63616c/tidepool/pull/7 (`tp/tckt_phasec-runner-path-fix`) — opencode PATH + cloud-init binary-dest fix. Mergeable. The two failed runs were driven off this branch, so the PATH fix **did not** resolve the agent-step failure.

## THE IMMEDIATE BLOCKER — debug why opencode-on-box fails
The agent step on the box fails before pushing/recording. Root cause unknown because of an **observability gap**: the reconciler maps `AgentFailed`→retry **without persisting the reason** (no error column in `runs`/`tickets`). The reason only exists in `tp run` stderr.
- **First action:** get the exact error. Ask `conductor` for the `tp run` stderr / `AgentFailed` (or `BoxFailed`) reason from its last run, OR re-instrument a run (SSH to the box, capture opencode/runner stderr) — but don't burn boxes blindly. Use **systematic-debugging**.
- Suspect areas: opencode binary/PATH on box (despite PR #7), `auth.json` JIT delivery over SSH, the `RUNNER_SCRIPT` embedded string (see below), git clone/push/auth on the box, the embedded `bun:sqlite`/SDK behaviour. RESEARCH.md documents opencode gotchas (avoid `session_diff` bug #20990; Codex sub has cost=0 so key proof on TOKENS not cost).

## Known correctness issues to fix as part of "done"
1. **`tp doctor` box_id check is too weak.** Fact #4 passes on ANY non-null `box_id` incl. LocalBoxMaker's local box (`box_fhgwags0wd`). So doctor would **falsely** report Phase C proven on a laptop run. Harden it to assert a **real Hetzner box** (persist hcloud numeric server id / provider tag on the run, assert that). TDD it. The conductor was asked to do this — **verify whether it actually landed.** Code: `src/doctor.ts`, `doctorVerdict`.
2. **Persist the AgentFailed/BoxFailed reason** to the store (observability) so failures are diagnosable post-hoc.
3. **`RUNNER_SCRIPT` is a ~106-line TS program inside a template literal** (`src/agent-runner.ts:237-343`), `cat >`'d to the box over SSH. Not type-checked, escaping-hell (nested Bun `$` literals), and the PATH bug hid there. Refactor to a real `src/worker/runner.ts` (type-checked) that's read-from-disk / `bun build`'d and uploaded. Do this as part of landing PR #7 cleanly.

## Decisions made THIS session (carry forward; some need doc updates — tenet 11)
- **Both repos PUBLIC + hardened.** `tidepool-testbed` and `tidepool` are now public. Both have: branch protection on `main` (required status check `check`, linear history, no force-push/deletions, enforce-admins), Actions default token read-only + no-Actions-PR-approval, fork-PR approval = all_external_contributors, 0 repo secrets. (Calum approved crossing the privacy tenet; GitHub Free blocked private branch protection.)
- **IDs are lowercase base36** (`src/ids.ts`), not base62 — required to match the commitlint/branch regex (`tckt_[0-9a-z]+`). DESIGN.md still says base62 → fix in docs.
- **Global guard edited:** the unconditional `gh pr create` ban was removed from `~/.claude/hooks/guard-worktree-git.sh` (kept the worktree-branch guard). Affects all Calum's projects.
- **Skills wired:** `.claude/skills` → `.agents/skills` symlink (gitignored); `effect-ts`, `axi`, `tdd` in the Skill registry. Instruct sub-agents to invoke `tdd` (strict vertical red→green), `effect-ts` (skip its `.repos/effect` clone prereq), `axi`.
- **Test runner:** `bun --bun vitest run` (vitest-on-Node can't load `bun:sqlite`); `+@effect/experimental`.
- **Process slip to avoid repeating:** a prior lead merged PR #2 while its `commitlint` job was red (101-char subject). GATE every merge on BOTH `check` AND `commitlint` green. Now that tidepool is public, its CI is free — use it.

## Process / standards (enforced)
- Green **merged PR** per change, no direct push to `main`. Branch `tp/<tckt_id>-slug`, work in a worktree (the guard blocks branch creation from the main checkout). Squash-merge (linear history). Commit + PR title must start with `#tckt_<lowercaseid> ` (commitlint), ≤100 chars.
- Effect-first (tenet 10), docs in same change (tenet 11). Crossing a tenet → stop + ask Calum.
- Boundaries: **≤5 Hetzner boxes ever, every worker box deleted, N=1.** Hetzner spend is authorized. Calum also said "run commands you need to unblock yourself" — but repo **visibility/access-control changes stay human-only** (hand Calum the command).

## Remaining work to mission DONE
1. Diagnose + fix the opencode-on-box failure (the actual blocker).
2. Harden `tp doctor` box_id check (real Hetzner proof) + TDD; verify conductor's version.
3. Drive a real Hetzner e2e: reset `tckt_qt6tbzn900` to `backlog` (`sqlite3 .tidepool/tidepool.sqlite "UPDATE tickets SET state='backlog',attempts=0 WHERE id='tckt_qt6tbzn900';"` from **repo root** — DB path is CWD-relative), `bun run src/cli.ts run` → real box → opencode pushes truncate → testbed PR → CI green → auto-merge → box deleted → `tp doctor` exit 0 on hardened check + non-zero usage → confirm 0 servers.
4. Persist failure reasons (observability).
5. Land PR #7 + a docs PR (tenet 11: update DESIGN/HANDOFF/AGENTS/GOALS — base36 ids, both repos public+hardened, doctor hardened, guard lift; note Phase C is the proven milestone).

## Longer-term design threads Calum raised (DO NOT capture into docs yet — he said so; context only)
- Keep Pulumi but possibly provision **all** boxes (incl. workers) via Pulumi Automation API vs the current direct-Hetzner-API workers. Tradeoff discussed: workers want direct-API for bulletproof teardown + no state-backend SPOF; main box/network/firewall suit Pulumi. Both already hide behind the `BoxMaker` interface (tenet 4).
- **Pulumi Cloud token is NOT actually required** — use a self-managed backend (Hetzner Object Storage S3, already offered by `infra/bootstrap/collect.sh`) or local; or skip Pulumi for a single management box and reuse HetznerBoxMaker.
- Day-2 ops: in-box updates (deps, tp version) should be a **self-converge/self-update systemd timer** pulling git desired-state (no SSH, no destroy); only image/shape changes roll a new box. The sqlite DB must live on a **separate Hetzner Volume marked protect/retain** + snapshots so box replacement never loses state.
- Tidepool is a **personal** control plane (not multi-tenant SaaS); Phase D bring-up has an irreducible one-time manual step (place the master age key on the box — can't be GitOps). Envisioned as a `tp up` one-time bootstrap, then `tp ticket add` steady-state.
- The current CLI conflates the daemon loop (`tp run`) with the engineer's client tool — worth splitting later.

## Environment / footguns
- Repo: `/Users/calum/code/github.com/0x63616c/tidepool`. The session cwd is a worktree (`.claude/worktrees/tckt_phasea-coverage`); **run `tp`/`tp run` from the repo root** — `DB_PATH` is hardcoded CWD-relative (`.tidepool/tidepool.sqlite`, gitignored).
- Bootstrap secrets in `~/.tidepool/bootstrap/` (hcloud_token, opencode-auth.json, age-*.key, ssh-tidepool) — **do not print/commit**. `secrets/tidepool.enc.yaml` is sops-encrypted (safe). opencode auth also at `~/.local/share/opencode/auth.json`.
- Tooling: bun 1.2.19, opencode 1.17.11, gh authed as `0x63616c`, sops/age present. `tp` CLI needs `bun link` to be on PATH.
- Hetzner: 0 servers currently. Box type CPX/x86 (CAX/ARM unavailable); location/type fallback nbg1→hel1→fsn1.

## Suggested skills (invoke as relevant)
- **superpowers:systematic-debugging** — for the opencode-on-box failure (the blocker). Diagnose before retrying.
- **tdd** (and superpowers:test-driven-development) — strict vertical red→green for the doctor hardening + any fix.
- **effect-ts** — all Effect/Schema/layer work (skip its `.repos/effect` clone prereq).
- **verify** — confirm the real run actually works (run the app, observe), don't just claim.
- **superpowers:dispatching-parallel-agents** / Agent tool — keep delegating; you're the lead.
- **axi** — if touching the `tp` CLI surface.
