---
id: tckt_003
title: add truncate
goal: "add truncate(s: string, n: number): string in src/string.ts — returns s unchanged if length <= n, else slices to n and appends '…'. Tested, including the boundary n === s.length."
priority: 3
target: tidepool-testbed
---
A second clean ticket to exercise the queue beyond a single item.
