# GOALS.md — the mission for Tidepool

Before anything: read `HANDOFF.md → DESIGN.md → RESEARCH.md → AGENTS.md → SKILLS.md`. The design is
LOCKED — execute, don't re-decide. Operate as **lead**: dispatch parallel agent teams (Workflow tool,
Opus, parallelize) + validators; preload the SKILLS.md skills + wire the Pulumi MCP first so Effect/TS
is idiomatic; keep Calum's context low; report concisely. Stack is locked (Effect-first — tenet 10,
one way of doing things). Cap = 1 (design-for-N). Tenets in AGENTS.md hold; crossing one → STOP + ask.

Calum has **authorized real spend** — you do NOT need permission for `pulumi up`, Hetzner boxes, etc.
Spend is bounded by guardrails (5-box project limit, reaper, Scope teardown, max-TTL): respect them.

---

## THE MISSION — ACHIEVED / PROVEN 2026-06-28

**A PR is merged into `tidepool-testbed@main` whose code was generated by opencode running ON a real
Hetzner worker box, having passed the full flow end-to-end** (claim → real box → agent → PR → CI green
→ review agent → auto-merge → box destroyed), proven by `tp doctor`. **Phase C (the mission) is
ACHIEVED/PROVEN (2026-06-28).** Outstanding item: **Phase D** (always-on cloud control plane).

Build in phases to de-risk (each is a checkpoint, not the finish). DONE = the Phase C check passes AND
all docs are current AND everything is pushed.

### Phase A — orchestrator core on fakes (no spend)
- `bun install` ok; `bun run check` exits 0 (biome + `tsc` + `vitest`), **0 failed, 0 skipped**.
- Reconciler suite PASSES (name them, show output): (a) happy path `backlog→…→done` on green fake
  CI+review; (b) red fake-CI → `in_progress` retry to cap → `failed`; (c) "deploy mid-task": an
  `in_progress` ticket with reattach handles is **resumed not restarted** after the reconciler is
  reconstructed.
- Adapters are Effect services/Layers behind the locked interfaces; `Fake*` = swapped layers.

### Phase B — real adapters, local execution (no cloud spend)
- `tidepool-testbed` created (`0x63616c/tidepool-testbed` — now **public, hardened**): pure-fn TS lib +
  vitest + identical rails (biome, commitlint, CI, `main` protected: required check `check`, linear
  history, no force-push, no deletions, enforce-admins; Actions `GITHUB_TOKEN` read-only; 0 repo
  secrets), pushed. (`tidepool` itself is also public + hardened with the same protection.)
- Real **GitHub `Forge`** (Octokit) + **opencode `AgentRunner`** (`@opencode-ai/sdk`, model
  `openai/gpt-5.4-mini`) + **`LocalBoxMaker`** (this machine). Drive `tckt_001` (slugify) → real PR →
  CI green → review approves → **auto-merged**.
- `tp doctor` exits 0 (local): `slugify` on `tidepool-testbed@main`, fresh-clone `bun run test` passes,
  run's sqlite `usage` row has **non-zero tokens**. Show output.

### Phase C — real Hetzner worker (cloud spend) — THE MISSION — ACHIEVED/PROVEN 2026-06-28
- **Real Hetzner `BoxMaker`** (workers are runtime cattle via the Hetzner API — NOT Pulumi): location+
  type fallback (`cpx22→cpx32…`, `nbg1→hel1→fsn1`) on `resource_unavailable`; Effect `Scope` teardown;
  label `managed_by=tidepool`; **reaper** + per-box **max-TTL**; box on a private network.
- Secrets in **sops** (`secrets/tidepool.enc.yaml`, recipients main-box+ci+break-glass) built from
  `~/.tidepool/bootstrap/`; the worker gets `auth.json` JIT over SSH; plaintext shredded.
- Drive a ticket end-to-end on a REAL worker box: box created (show id + private `10.x` IP), **opencode
  runs ON that box**, PR merged on `tidepool-testbed@main`, **box DELETED** (show project servers back
  to baseline). Spend guardrails **L1–L6** in code.
- `tp doctor` exits 0 and the run record shows the latest work-run **provider = `hetzner`** (proves
  Hetzner-generated, not local). Show output.

### DONE (mission)
Phase C check passes; **every worker box deleted** (workers → 0; the management box, if any, is
**persistent** and stays — never delete it); **all docs updated
to match what was built** (DESIGN/HANDOFF/AGENTS/GOALS — no stale/contradictory text, tenet 11); all
work committed (AGENTS.md commit format) and pushed; CI green.

### BOUNDARIES (hard)
≤5 boxes ever; **every created WORKER box MUST be deleted** (workers → 0 at the end); the **management
box is persistent — keep it, never reap/delete it** (reaper + TTL target `role=worker` only); **never
>1 worker concurrently** (N=1 — and it keeps the single-`auth.json` refresh safe); don't weaken/skip/delete
tests; Effect-first, no mixed idioms (tenet 10); update docs in the same change (tenet 11). No spend
permission needed — guardrails are the control.

---

## Phase D — always-on cloud control plane (separate; needs ONE thing from Calum)
Make the reconciler itself cloud-resident: **Pulumi (TS) main-box stack applied by `pulumi up` in
GitHub Actions** (GitOps), cpx12 EU on the private network, deny-all-inbound-except-SSH, cloud-init
(mise+bun+opencode, clone, inject age key, systemd reconciler + self-update timer). State in **Pulumi
Cloud**. **Blocker:** needs a **Pulumi Cloud account + access token** from Calum (the one cred the lead
can't self-create) — add it to `~/.tidepool/bootstrap/` + GH Actions. Not required for THE MISSION
(workers are API cattle; the reconciler can run locally to drive the Phase C run).
